<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.DarkActionBar"
        android:networkSecurityConfig="@xml/network_security_config">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".NetworkActivity" android:exported="false" />
        <activity android:name=".StorageActivity" android:exported="false" />
        <activity android:name=".SqlActivity" android:exported="false" />

        <!-- VULNERABILITY: Exported Activity — triggers android_hiddenui + android_hardcoded in code
             MobSF Rule (code): android_hiddenui (CWE-919, M1, storage-7)
             MobSF Rule (code): android_hardcoded (CWE-312, M9, storage-14) -->
        <activity
            android:name=".ExposedActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="com.test.reachability.ADMIN" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!-- VULNERABILITY: Exported Service — triggers android_logging in code
             MobSF Rule (code): android_logging (CWE-532, storage-3) -->
        <service
            android:name=".PhantomService"
            android:exported="true" />

    </application>

</manifest>